#ifndef __NM_CPPM_Utils
#define __NM_CPPM_Utils

#include <any>
#include <cstddef>
#include <string>
#include <vector>

std::size_t len(std::vector<std::any> vec);
std::size_t len(std::string str);

#endif // __NM_CPPM_Utils
